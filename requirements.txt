# --- Base runtime deps ---
click>=8.1
requests>=2.31
tenacity>=8.2

pydantic>=2.6
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
pyarrow>=14.0

datasets>=2.18
huggingface_hub>=0.23
joblib>=1.3

SQLAlchemy>=2.0
python-dateutil>=2.8
python-dotenv>=1.0

# --- Optional engines ---
# 若要啟用 BM25 相似度（COFACTS_SEARCH_ENGINE=bm25）
# rank-bm25>=0.2

# 若要啟用 SBERT 語意相似度（COFACTS_SEARCH_ENGINE=sbert）
# 建議 CPU 安裝；GPU 請改安裝對應輪子
# torch>=2.1
# sentence-transformers>=3.0
